import React from 'react'
import { View } from 'react-native'

const SendCroppedImage = () => {
    config = {
        url: 'https://',
        data: {
            firstName: 'Fred',
            lastName: 'Flintstone'
        }
    }

    return (
        <View>

        </View>
    )
}

export default SendCroppedImage
